{
  "name": "LATEST_PUBLICATION_YEAR_BY_ASSIGNEE",
  "description": "Latest publication year for an assignee.",
  "params_schema": {
    "assignee": { "type": "string", "required": true }
  },
  "cypher": "MATCH (a:Assignee {name:$assignee})<-[:HAS_ASSIGNEE]-(p:Patent)-[:PUBLISHED_IN]->(y:Year) RETURN a.name AS assignee, max(y.year) AS latest_year, count(DISTINCT p) AS patent_count LIMIT 1;",
  "result_schema": {
    "assignee": "Assignee name",
    "latest_year": "Latest year",
    "patent_count": "Patent count"
  },
  "ui": { "show": false, "examples": [] }
}
