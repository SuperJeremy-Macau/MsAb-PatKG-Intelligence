{
  "name": "CROSS_FIRST_DISCLOSURE_YEAR_BY_TARGETPAIR",
  "description": "First disclosure year by target pair.",
  "params_schema": {
    "tp_name": {
      "type": "string",
      "required": true
    }
  },
  "cypher": "MATCH (tp:TargetPair {name:$tp_name})<-[:HAS_TARGET_PAIR]-(p:Patent)-[:PUBLISHED_IN]->(y:Year)\nRETURN tp.name AS target_pair, min(y.year) AS first_year, count(DISTINCT p) AS patent_count\nLIMIT 1;",
  "result_schema": {
    "target_pair": "Target pair",
    "first_year": "First year",
    "patent_count": "Patent count"
  },
  "ui": {
    "show": true,
    "examples": [
      "某target pair首次公开年份是什么？"
    ]
  }
}