{
  "name": "DISTINCT_TARGETPAIR_COUNT_BY_ASSIGNEE",
  "description": "Count distinct target pairs for an assignee.",
  "params_schema": {
    "assignee": { "type": "string", "required": true }
  },
  "cypher": "MATCH (a:Assignee {name:$assignee})<-[:HAS_ASSIGNEE]-(p:Patent)-[:HAS_TARGET_PAIR]->(tp:TargetPair) RETURN a.name AS assignee, count(DISTINCT tp) AS distinct_targetpair_count LIMIT 1;",
  "result_schema": {
    "assignee": "Assignee",
    "distinct_targetpair_count": "Distinct target pair count"
  },
  "ui": { "show": false, "examples": [] }
}
