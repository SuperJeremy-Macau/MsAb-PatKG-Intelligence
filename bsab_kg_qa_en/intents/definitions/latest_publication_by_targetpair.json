{
  "name": "LATEST_PUBLICATION_BY_TARGETPAIR",
  "description": "Latest publication year for a target pair.",
  "params_schema": {
    "tp_name": { "type": "string", "required": true }
  },
  "cypher": "MATCH (tp:TargetPair {name:$tp_name})<-[:HAS_TARGET_PAIR]-(p:Patent)-[:PUBLISHED_IN]->(y:Year) RETURN tp.name AS target_pair, max(y.year) AS latest_year, count(DISTINCT p) AS patent_count LIMIT 1;",
  "result_schema": {
    "target_pair": "Target pair",
    "latest_year": "Latest year",
    "patent_count": "Patent count"
  },
  "ui": { "show": false, "examples": [] }
}
