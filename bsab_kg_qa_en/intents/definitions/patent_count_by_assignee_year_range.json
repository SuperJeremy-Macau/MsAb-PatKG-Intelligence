{
  "name": "PATENT_COUNT_BY_ASSIGNEE_YEAR_RANGE",
  "description": "Count patents for an assignee in a year range.",
  "params_schema": {
    "assignee": { "type": "string", "required": true },
    "start_year": { "type": "integer", "required": true },
    "end_year": { "type": "integer", "required": true }
  },
  "cypher": "MATCH (a:Assignee {name:$assignee})<-[:HAS_ASSIGNEE]-(p:Patent)-[:PUBLISHED_IN]->(y:Year) WHERE y.year >= $start_year AND y.year <= $end_year RETURN a.name AS assignee, $start_year AS start_year, $end_year AS end_year, count(DISTINCT p) AS patent_count LIMIT 1;",
  "result_schema": {
    "assignee": "Assignee",
    "start_year": "Start year",
    "end_year": "End year",
    "patent_count": "Patent count"
  },
  "ui": { "show": false, "examples": [] }
}
