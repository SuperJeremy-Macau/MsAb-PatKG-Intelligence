{
  "name": "PATENT_COUNT_BY_ASSIGNEE",
  "description": "Count BsAb patents for a given assignee.",
  "params_schema": {
    "assignee": { "type": "string", "required": true }
  },
  "cypher": "MATCH (a:Assignee {name:$assignee})<-[:HAS_ASSIGNEE]-(p:Patent) RETURN a.name AS assignee, count(DISTINCT p) AS patent_count LIMIT 1;",
  "result_schema": {
    "assignee": "Assignee name",
    "patent_count": "Patent count"
  },
  "ui": {
    "show": false,
    "examples": ["How many BsAb patents are assigned to 3sbio Inc?"]
  }
}
